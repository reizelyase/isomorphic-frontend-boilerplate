## This file will be included by our bin/dev.sh
## file and can be used to overload or add an ENV
## vars before our app starts

export DEBUGGER=false        # Options: [true, false]
export DEBUG_BRK=false      # Options: [true, false]
export NODE_ENV=production  # Options: [local, dev, staging, production]
export HOST=localhost
export PORT=8069
export APIPORT=8070
export APIHOST=localhost
